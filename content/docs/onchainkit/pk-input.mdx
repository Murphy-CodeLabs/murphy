---
title: PK Input
description: The PKInput component is an input field with support for inline public key validation.
icon: Key
---
<PreviewComponent placeholder="Enter a public key" v0JsonFileName="pk-input">
<PKInput placeholder="Enter your public key"/>
</PreviewComponent>
## Installation

<Steps>
    <Step>
        Install shadcn/ui input component
        ```bash
        npx shadcn@latest add input
        ```
    </Step>
    <Step>
        Install Murphy PKInput
        <InstallationCommands packageUrl={`${process.env.NEXT_PUBLIC_BASE_URL}/r/pk-input.json`}/>
    </Step>
</Steps>

## Basic Usage
```tsx
import {PKInput} from "@/components/ui/murphy/pk-input.tsx"
```
```tsx
<PKInput placeholder="Enter your public key"/>
```