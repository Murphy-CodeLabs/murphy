---
title: Token List
description: A table of tokens and prices with optional user balances.
icon: List
---

<PreviewComponent name={"Token List"} v0JsonFileName={"token-list"}>
  <TokenList assets={[]} className="max-w-md" />
</PreviewComponent>

## Installation

<Steps>
    <Step>
        Install dependencies

        Start by installing required Murphis dependencies

        ```bash
        npx shadcn add table
        ```

    </Step>

    <Step>
    Add Murphy Token Icon
    <InstallationCommands
      packageUrl={`${process.env.NEXT_PUBLIC_BASE_URL}/r/token-icon.json`}
    />

    </Step>
    
    <Step>
    Add Murphy Token List

    <InstallationCommands
      packageUrl={`${process.env.NEXT_PUBLIC_BASE_URL}/r/token-list.json`}
    />
    </Step>
</Steps>

## Basic Usage
    ```tsx
    import {TokenList} from "@/components/ui/murphy/token-list.tsx"
    ```
    ```tsx
    <TokenList assets={assets} showBalances={false} />
    ```

## Props

| Name           | Type                        | Default |
| -------------- | --------------------------- | ------- |
| `assets`       | `SolAsset[]`                | `[]`    |
| `showBalances` | `boolean`                   | `true`  |
| `onClick`      | `(asset: SolAsset) => void` |         |
