---
title: TxnSettings
description: The TxnSettings component is a popover that allows users to set the transaction priority, fee cap, and other transaction settings.
icon: TrendingUp
---

<PreviewComponent name={"TxnSettings"} v0JsonFileName={"txn-settings"}>
  <TxnSettings />
</PreviewComponent>
## Installation

<Steps>
  <Step>
      Install shadcn/ui popover, toggle group, input, and button components

      ```bash
      npx shadcn@latest add popover toggle-group input button
      ```
  </Step>
    <Step>
      Install Murphis TxnSettings

      <InstallationCommands packageUrl={`${process.env.NEXT_PUBLIC_BASE_URL}/r/txn-settings.json`}/>
  </Step>
  <Step>

  ```tsx
  import { TxnSettingsProvider } from "@/components/murphis/txn-settings";

export default function Layout({
    children,
    }: {
      children: React.ReactNode;
    }) {
  return (
    <div>
      <TxnSettingsProvider>
        {children}
      </TxnSettingsProvider>
    </div>
  )
}
  ```
  </Step>
</Steps>


## Basic Usage
```tsx
import {TxnSettings} from "@/components/ui/murphy/pk-input.tsx" 
```

```tsx
<TxnSettings />
```

## Props

| Name              | Type                        | Default                                                                  |
| ----------------- | --------------------------- | ------------------------------------------------------------------------ |
| `trigger`         | `React.ReactNode`           |`<Button variant="outline" size="icon"> <IconSettings size={16}></Button>`|
